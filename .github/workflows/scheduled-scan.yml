name: ApplyPilot Scheduled Scan

on:
  workflow_dispatch:
<<<<<<< HEAD

  schedule:
    # Every 6 hours. GitHub cron uses UTC.
    - cron: "17 */6 * * *"
=======
  schedule:
    # GitHub cron is UTC. This checks due sources every hour at minute 17.
    - cron: "17 * * * *"
>>>>>>> 1b00594 (Upgrade ApplyPilot AI to version 2)

permissions:
  contents: read

concurrency:
  group: applypilot-scheduled-scan
  cancel-in-progress: false

jobs:
  scan-jobs:
    name: Scan career sources
    runs-on: ubuntu-latest
    timeout-minutes: 55
<<<<<<< HEAD

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      CRAWLER_USER_AGENT: ApplyPilotAI/1.0
      RESPECT_ROBOTS_TXT: "true"
      CRAWLER_MAX_LINKS_PER_SOURCE: "30"
      CRAWLER_FETCH_MODE: "html"
=======
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      OPENAI_MODEL: ${{ vars.OPENAI_MODEL || 'gpt-5.6-luna' }}
      CRAWLER_USER_AGENT: ApplyPilotAI/2.0
      RESPECT_ROBOTS_TXT: "true"
      CRAWLER_MAX_LINKS_PER_SOURCE: "40"
      CRAWLER_FETCH_MODE: ${{ vars.CRAWLER_FETCH_MODE || 'html' }}
>>>>>>> 1b00594 (Upgrade ApplyPilot AI to version 2)

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          package-manager-cache: false

      - name: Install dependencies
        run: npm install --include=dev --no-audit --no-fund

      - name: Generate Prisma Client
        run: npx prisma generate

<<<<<<< HEAD
=======
      - name: Install Chromium when browser rendering is enabled
        if: ${{ vars.CRAWLER_FETCH_MODE == 'playwright' }}
        run: npx playwright install --with-deps chromium

>>>>>>> 1b00594 (Upgrade ApplyPilot AI to version 2)
      - name: Run ApplyPilot source scanner
        env:
          NODE_ENV: production
        run: npm run scan:once
