services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: applypilot
      POSTGRES_PASSWORD: applypilot
      POSTGRES_DB: applypilot
    ports:
      - "5432:5432"
    volumes:
      - applypilot_postgres:/var/lib/postgresql/data

volumes:
  applypilot_postgres:
